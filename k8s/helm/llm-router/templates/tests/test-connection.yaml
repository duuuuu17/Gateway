apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "llm-router.fullname" . }}-test-connection"
  labels:
    {{- include "llm-router.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "llm-router.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
